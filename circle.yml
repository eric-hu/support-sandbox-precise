#machine:
  #pre:
    #- sudo service mysql stop

#checkout:
  #override:
    #- git pull --all --unshallow

test:
  override:
    - exit 0
deployment:
  pre:
    branch: master
    commands:
      - exit 0
