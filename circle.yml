machine:
  ruby:
    version: 2.2.0

dependencies:
  pre:
    - bundle --version
    - rvm all-gemsets do "echo Y | gem uninstall bundler"
    - which bundle
    - gem install bundler -v '1.10.6'
    - bundle --version
