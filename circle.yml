dependencies:
  override:
    #- curl -sSL https://ftp.ruby-lang.org/pub/ruby/ruby-2.1.9.tar.bz2 -o ruby-2.1.9.tar.bz2
    #- mv ruby-2.1.9.tar.bz2 $rvm_path/archives/
    - mkdir ~/rvm_binaries || true
    - cp ~/rvm_binaries/ruby-2.1.9.tar.bz2 $rvm_path/archives || true
    - rvm install 2.1.9
    - cd ~/rvm_binaries && rvm prepare 2.1.9
    - ls ~/rvm_binaries


  cache_directories:
    - ~/rvm_binaries

test:
  override:
    - exit 0
