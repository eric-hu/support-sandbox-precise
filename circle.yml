machine:
  ruby:
    version: 2.2.0

dependencies:
  pre:
    - bundle --version
    - rvm all-gemsets do gem uninstall bundler --executables
    - which bundle
    - gem install bundler -v '1.10.6'
    - bundle --version
